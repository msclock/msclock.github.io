<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>配置cgywin环境</title>
    <url>/posts/3617517984/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><a id="more"></a><h2 id="install-Cygwin"><a href="#install-Cygwin" class="headerlink" title="install Cygwin"></a>install Cygwin</h2><ul><li>通过运行<a href="https://cygwin.com/setup-x86_64.exe" target="_blank" rel="noopener">setup-x86_64.exe（64位安装）</a>或<a href="https://cygwin.com/setup-x86.exe" target="_blank" rel="noopener">setup-x86.exe（32位安装）</a>安装Cygwin</li><li>使用安装程序执行<a href="https://cygwin.com/install.html" target="_blank" rel="noopener">新安装</a>或<a href="https://cygwin.com/install.html" target="_blank" rel="noopener">更新现有安装</a>。</li><li>请记住，发行版中的单个包是与DLL分开更新的，所以Cygwin DLL版本对于一般的Cygwin发行版本号来说没有用。</li></ul><h2 id="domestic-source-address"><a href="#domestic-source-address" class="headerlink" title="domestic source address"></a>domestic source address</h2><ul><li><p>网易</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;mirrors.163.com&#x2F;cygwin&#x2F;</span><br></pre></td></tr></table></figure></li></ul><ul><li><p>中科大</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;cygwin&#x2F;</span><br></pre></td></tr></table></figure></li></ul><ul><li><p>搜狐</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;mirrors.sohu.com&#x2F;cygwin&#x2F;</span><br></pre></td></tr></table></figure></li></ul><h2 id="apt-cyg"><a href="#apt-cyg" class="headerlink" title="apt-cyg"></a>apt-cyg</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wget https://raw.githubusercontent.com/transcode-open/apt-cyg/master/apt-cyg</span><br><span class="line">mv apt-cyg /bin/apt-cyg</span><br><span class="line">chmod +x /bin/apt-cyg</span><br><span class="line">apt-cyg mirror http://mirrors.sohu.com/cygwin/</span><br></pre></td></tr></table></figure><h2 id="compilation-env"><a href="#compilation-env" class="headerlink" title="compilation env"></a>compilation env</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-cyg install gcc-core gcc-g++ gdb make autoconf automake libboost-devel</span><br></pre></td></tr></table></figure><h2 id="management"><a href="#management" class="headerlink" title="management"></a>management</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-cyg install openssh openssl binutils util-linux bash-completion procps inetutils <span class="built_in">bind</span>-utils</span><br></pre></td></tr></table></figure><h2 id="common-tools"><a href="#common-tools" class="headerlink" title="common tools"></a>common tools</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-cyg install git wget curl vim tree</span><br></pre></td></tr></table></figure><h2 id="python"><a href="#python" class="headerlink" title="python"></a>python</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt-cyg install python python-ipython python-pip python-setuptools</span><br><span class="line">apt-cyg install python3 python3-ipython python3-pip python3-setuptools</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash">pip配置</span></span><br><span class="line">mkdir ~/.pip</span><br><span class="line">touch ~/.pip/pip.conf</span><br><span class="line">echo -e "[global]\nindex-url = https://pypi.tuna.tsinghua.edu.cn/simple" &gt; ~/.pip/pip.conf</span><br><span class="line">python -m pip install --upgrade pip</span><br><span class="line">pip install ipython</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash">语法检查</span></span><br><span class="line">pip install --user flake8</span><br><span class="line"><span class="meta">#</span><span class="bash">自动代码提示</span></span><br><span class="line">pip install --user jedi</span><br><span class="line"><span class="meta">#</span><span class="bash">自动导入模块</span></span><br><span class="line">pip install --user isort</span><br><span class="line"><span class="meta">#</span><span class="bash">代码格式化</span></span><br><span class="line">pip install --user yapf</span><br></pre></td></tr></table></figure><h2 id="man"><a href="#man" class="headerlink" title="man"></a>man</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-cyg install help2man man-db man-pages-posix</span><br></pre></td></tr></table></figure><h2 id="zsh"><a href="#zsh" class="headerlink" title="zsh"></a>zsh</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-cyg install zsh</span><br><span class="line">sh -c <span class="string">"<span class="variable">$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)</span>"</span></span><br></pre></td></tr></table></figure><h2 id="npm"><a href="#npm" class="headerlink" title="npm"></a>npm</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npm.taobao.org</span><br><span class="line">npm i -g tldr</span><br><span class="line">tldr --update</span><br><span class="line">npm i -g cnpm --registry=https://registry.npm.taobao.org</span><br><span class="line">npm i -g yarn</span><br><span class="line">yarn config <span class="built_in">set</span> registry https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -qO- https://raw.github.com/ma6174/vim/master/setup.sh | sh -x</span><br></pre></td></tr></table></figure><h2 id="references"><a href="#references" class="headerlink" title="references"></a>references</h2><ul><li><a href="https://www.cnblogs.com/zfc2201/p/10164543.html" target="_blank" rel="noopener">https://www.cnblogs.com/zfc2201/p/10164543.html</a></li></ul>]]></content>
      <categories>
        <category>environment</category>
        <category>cgywin</category>
      </categories>
  </entry>
  <entry>
    <title>函数指针</title>
    <url>/posts/1118485629/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="指向普通函数的指针"><a href="#指向普通函数的指针" class="headerlink" title="指向普通函数的指针"></a>指向普通函数的指针</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">int_add</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (a+b);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">int_sub</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (a-b);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">int</span> (*int_operator)(<span class="keyword">int</span>, <span class="keyword">int</span>) = int_add;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;int_operator(<span class="number">4</span>, <span class="number">5</span>)&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// output 9</span></span><br><span class="line">    int_operator = int_sub;</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;int_operator(<span class="number">4</span>, <span class="number">5</span>)&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// output -1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上例中，<code>int_operator</code>会被编译器解释成类型<code>int(*)(int, int)</code>的一个指针。</p><p>调用方式还可以写作：<code>(*int_operator)(4, 5)</code>，这样的好处是让人一眼就能看到<code>int_operator</code>是一个函数指针。<strong>注意：函数指针和指向函数的返回值的类型和参数都必须严格一致；</strong></p><a id="more"></a><h2 id="函数指针数组"><a href="#函数指针数组" class="headerlink" title="函数指针数组"></a>函数指针数组</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> (*pFuncArray[<span class="number">10</span>])();</span><br></pre></td></tr></table></figure><p><code>[]</code>的优先级高于<code>*</code>，该语句将<code>pFuncArray</code>声明为拥有10个元素的数组，每一个元素都是指向一个函数的函数指针，该函数没有参数，返回值类型为<code>int</code>；</p><p>注意不能写作：<code>int ((*pFuncArray)[10])()</code>，这样会产生编译错误；<code>(*pFuncArray)[10]</code>表明了<code>pFuncArray</code>是一个指针，该指针指向一个<code>含有 10个元素的数组</code>；其类型为<code>int()()</code>，显然，编译不能通过。</p><p>将上面的声明转换为<code>typedef</code>格式，会使程序可读性增加：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span><span class="params">(*pFunc)</span><span class="params">()</span></span>;</span><br><span class="line">pFunc pFuncArray[<span class="number">10</span>];</span><br></pre></td></tr></table></figure><p>如果需要调用其中的第三个函数，那么调用方式为：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">pFuncArray[<span class="number">2</span>]();</span><br></pre></td></tr></table></figure><h2 id="指向‘函数指针数组’的指针"><a href="#指向‘函数指针数组’的指针" class="headerlink" title="指向‘函数指针数组’的指针"></a>指向‘函数指针数组’的指针</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">cmp_len</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *str1, <span class="keyword">const</span> <span class="keyword">char</span> *str2)</span></span></span><br><span class="line"><span class="function"></span>&#123;<span class="keyword">return</span> ((<span class="keyword">int</span>)<span class="built_in">strlen</span>(str1) - (<span class="keyword">int</span>)<span class="built_in">strlen</span>(str2));&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">cmp_str</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *str1, <span class="keyword">const</span> <span class="keyword">char</span> *str2)</span></span></span><br><span class="line"><span class="function"></span>&#123;<span class="keyword">return</span> <span class="built_in">strcmp</span>(str1, str2);&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span><span class="params">(*PCMP_FUNC)</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>*, <span class="keyword">const</span> <span class="keyword">char</span>*)</span></span>;</span><br><span class="line"> </span><br><span class="line">PCMP_FUNC pCmpFuncs[<span class="number">2</span>] =</span><br><span class="line">&#123;</span><br><span class="line">    cmp_len,</span><br><span class="line">    cmp_str,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 声明指向pCmpFuncs的指针</span></span><br><span class="line">PCMP_FUNC (*ppCmps)[<span class="number">2</span>] = &amp;pCmpFuncs;</span><br></pre></td></tr></table></figure><p>声明分解说明如下：</p><ul><li><p><code>(*ppCmps)</code>：表明<code>ppCmps</code>是一个指针;</p></li><li><p><code>(*ppCmps)[2]</code>：后面紧跟<code>[2]</code>，表明<code>ppCmps</code>是一个指向‘两个元素数组’的指针</p></li><li><p><code>PCMP_FUNC</code>表明了该数组元素的类型，它是指向函数的指针，返回值为<code>int</code>，有两个<code>const char*</code>类型的参数；</p></li><li><p>实际上语句<code>PCMP_FUNC (*ppCmps)[2] = &amp;pCmpFuncs;</code>将会被编译器解释为：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> (*(*ppCmps)[<span class="number">2</span>])(<span class="keyword">const</span> <span class="keyword">char</span>*, <span class="keyword">const</span> <span class="keyword">char</span>*) = &amp;pCmpFuncs;</span><br></pre></td></tr></table></figure><p><code>int (*)(const char*, const char *)</code>：表明了该数组元素的类型，它是指向函数的指针，返回值为<code>int</code>，有两个<code>const char*</code>类型的参数；</p></li></ul><h2 id="函数指针与类"><a href="#函数指针与类" class="headerlink" title="函数指针与类"></a>函数指针与类</h2><p>在<code>C++</code>语言中，使用函数指针可以指向类的一个成员函数或变量，虽然这种用法很少能用到，不过了解一下还是有点必要的。</p><ul><li><p>为了支持这类指针，<code>C++</code>有三个特殊的运算法符：<code>::*，.-&gt;，.*；</code></p></li><li><p><strong>指向成员函数的指针</strong>必须与向其赋值的函数类型匹配，这包括：</p><ul><li>参数的类型和个数；</li><li>返回值类型；</li><li>它所属的类型；</li></ul></li><li><p><strong>指向成员变量的指针</strong>必须与向其赋值的变量类型匹配，这包括</p><ul><li>变量类型；</li><li>它所属的类型；</li></ul></li><li><p>成员函数和变量必须被绑定到一个对象或者指针上，然后才能得到调用对象的<code>this</code>指针，然后才能指向成员变量或函数；</p></li><li><p>类<code>AClass</code>的成员变量<code>int m_iValue</code>，其完整类型是：<code>int AClass::m_iValue</code>；</p></li><li><p>类<code>AClass</code>的成员函数<code>int Add(int)</code>，其完整类型是：<code>int AClass::Add(int)</code>;</p></li></ul><p><strong>注意：指向类的静态变量或静态成员函数的指针和普通成员不同;</strong></p><h3 id="指向类成员变量的指针"><a href="#指向类成员变量的指针" class="headerlink" title="指向类成员变量的指针"></a>指向类成员变量的指针</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AClass</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Add</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;m_iValue += a;&#125;</span><br><span class="line">    <span class="keyword">int</span>  m_iValue;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    AClass a;</span><br><span class="line">   <span class="comment">// 声明并指向AClass的一个成员变量的指针</span></span><br><span class="line">    <span class="keyword">int</span> AClass::*pValue = &amp;AClass::m_iValue;</span><br><span class="line">    <span class="comment">// 或者如下方式：</span></span><br><span class="line">    <span class="comment">// int AClass::*pValue;// 指针变量声明</span></span><br><span class="line">    <span class="comment">// pValue = &amp;AClass::m_iValue;// 指向A的m_iValue成员</span></span><br><span class="line">    a.*pValue = <span class="number">4</span>; <span class="comment">// 使用方式，赋值</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;a.m_iValue&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 输出4</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="指向类成员函数的指针"><a href="#指向类成员函数的指针" class="headerlink" title="指向类成员函数的指针"></a>指向类成员函数的指针</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AClass</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Add</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;m_iValue += a;&#125;</span><br><span class="line">    <span class="keyword">int</span>  m_iValue;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 指向类AClass成员函数的指针的声明方式</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(AClass::*pAFunc)</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// 声明一个指向AClass::Add()的函数指针</span></span><br><span class="line">pAFunc pAAdd = &amp;(AClass::Add);</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    AClass a;</span><br><span class="line">    <span class="comment">// 声明并指向AClass的一个成员变量的指针</span></span><br><span class="line">    <span class="keyword">int</span> AClass::*pValue = &amp;AClass::m_iValue;</span><br><span class="line">    <span class="comment">// 或者如下方式：</span></span><br><span class="line">    <span class="comment">// int AClass::*pValue;// 指针变量声明</span></span><br><span class="line">    <span class="comment">// pValue = &amp;AClass::m_iValue;// 指向A的m_iValue成员</span></span><br><span class="line">    a.*pValue = <span class="number">4</span>; <span class="comment">// 使用方式，赋值</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;a.m_iValue&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 输出</span></span><br><span class="line">    (a.*pAAdd)(<span class="number">5</span>); <span class="comment">// 指向成员函数指针的调用方式</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;a.m_iValue&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 输出</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="指向类静态成员的指针"><a href="#指向类静态成员的指针" class="headerlink" title="指向类静态成员的指针"></a>指向类静态成员的指针</h3><p>类的静态成员属于该类的全局对象和函数，并不需要<em>this</em>指针；因此指向类静态成员的指针声明方式和普通指针相同。</p><p>类指针和普通指针的声明和调用方式完全相同；唯一的不同就是设置指向的对象时，仍然需要类信息，这一点和指向普通成员的指针相同。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AClass</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Add</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;m_iValue += a;&#125;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span>  m_iValue;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">int</span> AClass::m_iValue;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span><span class="params">(*pAAdd)</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">int</span> _tmain(<span class="keyword">int</span> argc, _TCHAR* argv[])</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 声明并指向AClass的一个静态成员变量的指针</span></span><br><span class="line">    <span class="keyword">int</span> *pValue = &amp;AClass::m_iValue;</span><br><span class="line">    <span class="comment">// 或者如下方式：</span></span><br><span class="line">    <span class="comment">// int *pValue;// 指针变量声明</span></span><br><span class="line">    <span class="comment">// pValue = &amp;AClass::m_iValue;// 指向A的m_iValue成员</span></span><br><span class="line">    *pValue = <span class="number">4</span>; <span class="comment">// 使用方式同普通指针，赋值</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;*pValue&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 输出</span></span><br><span class="line">    pAAdd p = &amp;AClass::Add;</span><br><span class="line">    p(<span class="number">5</span>); <span class="comment">// 调用方式同普通函数指针</span></span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;*pValue&lt;&lt;<span class="built_in">endl</span>; <span class="comment">// 输出</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>C中的宏</title>
    <url>/posts/3268695028/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="特殊宏"><a href="#特殊宏" class="headerlink" title="特殊宏"></a>特殊宏</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">__DATE__		: 在原文件中插入当前的编写日期</span><br><span class="line">__TIME__		: 在源文件中插入当前的编辑时间</span><br><span class="line">__cplusplus__	: 在源文件中编写c++程序时该标识被定义</span><br><span class="line">__FILE__		:文件名</span><br><span class="line">__LINE__		:行数</span><br><span class="line">__func__		:所在函数</span><br><span class="line">__FUNCTION__	:</span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="VA-ARGS"><a href="#VA-ARGS" class="headerlink" title="__VA_ARGS"></a><strong>__VA_ARGS</strong></h2><p>用于实现变参函数，将函数宏的形参列表最后的参数用省略号（…）表示即实现了变参函数。<code>__VA_ARGS__</code>用于在宏替换部分中，表明省略号代表什么。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PR(...) printf(__VA__ARGS__);    <span class="comment">//Note：需编译器支持__VA__ARGS__</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    	<span class="keyword">int</span> age = <span class="number">26</span>;</span><br><span class="line">    	<span class="keyword">int</span> weight = <span class="number">75</span>;</span><br><span class="line">    	<span class="keyword">float</span> BMI = <span class="number">26.4</span>;</span><br><span class="line">    	PR(<span class="string">"oh mashuai"</span>);</span><br><span class="line">    	PR(<span class="string">"Age:%d, weight:%d,BMI:%d"</span>,age,weight,BMI);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>##__VA_ARGS__</code> 宏前面加上<code>##</code>的作用在于，当可变参数的个数为0时，这里的<code>##</code>起到把前面多余的”<code>,</code>“去掉的作用,否则会编译出错。</strong></p><p>一般这个用在调试信息上多一点.</p><h2><a href="#" class="headerlink" title="#"></a>#</h2><p>表示将参数转换为字符串输出：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> P(A) printf(<span class="meta-string">"%s:%d.\r\n"</span>,#A,A);</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> b=<span class="number">2</span>;</span><br><span class="line">    P(a);</span><br><span class="line">    P(b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果为：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">a:1</span><br><span class="line">b:2</span><br></pre></td></tr></table></figure><h2 id="-1"><a href="#-1" class="headerlink" title="##"></a>##</h2><p>用于类函数宏的替换部分，也可以用于对象宏的替换部分。主要用于将两个语言符号组成单个语言符号，为宏扩展提供一种连接实际变元的手段；</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> XNAME(n) X##n        <span class="comment">//将定元X和变元n组合，列出X1, X2, X3 ...</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRINT_XN(n) printf(<span class="meta-string">"X"</span>#n<span class="meta-string">"=%d\n"</span>,X##n);</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">XNAME</span><span class="params">(<span class="number">1</span>)</span></span>=<span class="number">10</span>;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">XNAME</span><span class="params">(<span class="number">2</span>)</span></span>=<span class="number">20</span>;</span><br><span class="line">    <span class="keyword">int</span> X3=<span class="number">30</span>;</span><br><span class="line"> </span><br><span class="line">    PRINT_XN(<span class="number">1</span>);</span><br><span class="line">    PRINT_XN(<span class="number">2</span>);</span><br><span class="line">    PRINT_XN(<span class="number">3</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>C内存管理</title>
    <url>/posts/889578147/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><p>C 语言主要提供 <code>malloc</code>、<code>realloc</code>、<code>calloc</code>、<code>alloca</code> 与 <code>aligned_alloc</code> 等内存分配函数来<br>实现对内存的分配功能。</p><h2 id="malloc"><a href="#malloc" class="headerlink" title="malloc"></a>malloc</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">malloc</span> <span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span>;</span><br></pre></td></tr></table></figure><p>该函数用于从堆中分配内存空间，内存分配大小为 size。如果内存分配成功，则返回首地<br>址；如果内存分配失败，则返回 NULL。</p><h2 id="calloc"><a href="#calloc" class="headerlink" title="calloc"></a>calloc</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">calloc</span> <span class="params">(<span class="keyword">size_t</span> num, <span class="keyword">size_t</span> <span class="built_in">size</span> )</span></span>;</span><br></pre></td></tr></table></figure><p>该函数用于从堆中分配 <code>num</code> 个相邻的内存单元，每个内存单元的大小为 <code>size</code>。如果内存<br>分配成功则返回第一个内存单元的首地址；否则内存分配失败，则返回 <code>NULL</code>。<br>从功能上看，<code>calloc</code> 函数与语句<code>malloc（num*size)</code>的效果极其相似。但不同的是，<br>在使用 <code>calloc</code>函数分配内存时，会将内存内容初始化为 0。</p><a id="more"></a><h2 id="realloc"><a href="#realloc" class="headerlink" title="realloc"></a>realloc</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">realloc</span> <span class="params">(<span class="keyword">void</span> * ptr, <span class="keyword">size_t</span> <span class="built_in">size</span> )</span></span>;</span><br></pre></td></tr></table></figure><p>该函数用于更改已经配置的内存空间，它同样是从堆中分配内存的。当程序需要扩大一<br>块内存空间时，<code>realloc</code>函数<strong>试图直接从堆上当前内存段后面的字节中获得更多的内存空</strong><br><strong>间</strong>，即它将首先判断当前的指针是否有足够的连续存储空间，如果有，则扩大<code>ptr</code> 指向的<br>地址，并且将<code>ptr</code>返回（返回原指针）；如果当前内存段后面的空闲字节不够，那么将先<br>按照 <code>size</code> 指定的大小分配空间（使用堆上第一个能够满足这一要求的内存块），并将原有<br>数据从头到尾拷贝到新分配的内存区域，然后释放原来 <code>ptr</code> 所指内存区域，同时返回新分<br>配的内存区域的首地址，即重新分配存储器块的地址。</p><p>需要注意的是，参数 <code>ptr</code> 为指向先前由 <code>malloc</code>、<code>calloc</code> 与 <code>realloc</code> 函数所返回的内存指<br>针，而参数 <code>size</code> 为新分配的内存大小，其值可比原内存大或小。其中：</p><ul><li>如果 <code>size</code> 值比原分配的内存空间小，内存内容不会改变（即新内存保持原内存的内容），且返回的指针为原来内存的首地址（即 <code>ptr</code>）。</li><li>如果 <code>size</code> 值比原分配的内存空间大，则 <code>realloc</code> 不一定会返回原来的指针，原内存的内容保持不变，但新多出的内存则设为初始值。</li><li>最后，如果内存分配成功，则返回首地址；如果内存分配失败，则返回 <code>NULL</code>。</li></ul><h2 id="alloca"><a href="#alloca" class="headerlink" title="alloca"></a>alloca</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">alloca</span> <span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span>;</span><br></pre></td></tr></table></figure><p>相对与 <code>malloc</code>、<code>calloc</code> 与 <code>realloc</code> 函数，函数 <code>alloca</code> 是<strong>从栈中分配内存空间</strong>，内存分配大小为 <code>size</code>。如果内存分配成功，则返回首地址；如果内存分配失败，则返回 <code>NULL</code>。也正因为函数 <code>alloca</code> 是从栈中分配内存空间，因此它会自动释放内存空间，而无需手动释放。</p><h2 id="aligned-alloc"><a href="#aligned-alloc" class="headerlink" title="aligned_alloc"></a>aligned_alloc</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">aligned_alloc</span> <span class="params">(<span class="keyword">size_t</span> alignment,<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span>;</span><br></pre></td></tr></table></figure><p>该函数属于 <strong>C11 标准</strong>提供的新函数，用于<strong>边界对齐的动态内存分配</strong>。该函数按照参数 <code>alignment</code> 规定的对齐方式为对象进行动态存储分配 <code>size</code> 个 <code>size_t</code> 类型的存储单元。如果内存分配成功，则返回首地址；否则内存分配失败，则返回 <code>NULL</code>。</p><p>相对于 <code>malloc</code> 函数，<code>aligned_alloc</code> 函数保证了返回的地址是能对齐的，同时也要求 size 参数是 <code>alignment</code> 参数的整数倍。从表面上看，函数 <code>calloc</code> 相对 <code>malloc</code> 更接近 <code>aligned_alloc</code>，但 <code>calloc</code> 函数比 <code>aligned_alloc</code> 函数多了一个动作，那就是会将内存内容初始化为 <code>0</code>。</p><h2 id="内存分配函数的返回值"><a href="#内存分配函数的返回值" class="headerlink" title="内存分配函数的返回值"></a>内存分配函数的返回值</h2><table><thead><tr><th align="center">函数名</th><th align="center">成功返回</th><th align="center">失败返回</th><th align="center">errno</th></tr></thead><tbody><tr><td align="center">malloc</td><td align="center">指向被分配内存的指针</td><td align="center">NULL</td><td align="center">ENOMEM</td></tr><tr><td align="center">aligned_alloc</td><td align="center">指向被分配内存的指针</td><td align="center">NULL</td><td align="center">ENOMEM</td></tr><tr><td align="center">calloc</td><td align="center">指向被分配内存的指针</td><td align="center">NULL</td><td align="center">ENOMEM</td></tr><tr><td align="center">realloc</td><td align="center">指向重新分配内存的指针</td><td align="center">NULL</td><td align="center">ENOMEM</td></tr></tbody></table><p>在调用表 1 中的这些内存分配函数时，必须进行返回值检查，以便能够及时得到内存分配是否成功与失败（如果分配失败则返回 <code>NULL</code> 指针），这样也可以避免因为内存分配错误而导致的不可预知和意外程序行为发生，如下面的示例代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="number">100</span>);</span><br><span class="line"><span class="keyword">if</span> (!p)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/*处理内存分配错误，并返回错误状态*/</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>除通过使用<code>“if（p==NULL）”</code>或者<code>“if（p！=NULL）”</code>语句进行简单防错处理之外，如果指针 <code>p</code> 是函数的参数，那么还可以在函数的入口处用 <code>assert(p !=NULL)</code> 进行检查，从而避免发生内存分配未成功却使用了它的情况。</p><h2 id="内存资源的分配与释放"><a href="#内存资源的分配与释放" class="headerlink" title="内存资源的分配与释放"></a>内存资源的分配与释放</h2><p>内存资源的分配与释放应该限定在同一模块或者同一抽象层内进行，在 C 语言中，如果内存的分配和释放在不同的模块或抽象层内，不仅会加大程序员追踪内存块生命周期的负担，而且可能会导致内存泄漏、内存双重释放（<code>double-free</code>）、非法访问已经释放的内存、写入已释放或未分配的内存区域等问题。看下面一段示例代码：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN_MEM_SIZE 10</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">CompareMemorySize</span><span class="params">(<span class="keyword">char</span> *p, <span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">size</span> &lt; MIN_MEM_SIZE)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">free</span>(p);</span><br><span class="line">        p = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">AllocMemory</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="built_in">size</span>);</span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (CompareMemorySize(p, <span class="built_in">size</span>) == <span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">free</span>(p);</span><br><span class="line">        p = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="built_in">free</span>(p);</span><br><span class="line">    p = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的示例代码中，p 的内存是在 <code>AllocMemory</code> 函数中进行分配的，然后再将它通过语句“<code>CompareMemorySize(p,size)</code>”传给 <code>CompareMemorySize</code> 函数。在 <code>CompareMemorySize</code> 函数中，首先通过语句“<code>if(size&lt;MIN_MEM_SIZE)</code>”检查 <code>p</code> 所分配的内存长度，如果内存长度小于最小值<code>(MIN_MEM_SIZE)</code>，则释放 <code>p</code>。</p><p>然后，再将 <code>CompareMemorySize</code> 函数的返回值“<code>-1</code>”返回给调用者 <code>AllocMemory</code> 函数。在 <code>AllocMemory</code> 函数中执行语句“<code>if(CompareMemorySize(p,size)==-1)</code>”条件成立，<strong>再次释放 p</strong>。</p><p>很显然，这样不仅违背了“内存资源的分配与释放应该限定在同一模块或者同一抽象层内进行”的原则，同时导致了内存的双重释放。因此，需要对代码做如下修改：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN_MEM_SIZE 10</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">CompareMemorySize</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">size</span> &lt; MIN_MEM_SIZE)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">AllocMemory</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="built_in">size</span>);</span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;    </span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (CompareMemorySize(<span class="built_in">size</span>) == <span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">free</span>(p);</span><br><span class="line">        p = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="built_in">free</span>(p);</span><br><span class="line">    p = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在，函数 <code>CompareMemorySize</code> 与 <code>AllocMemory</code> 的职责很清楚了。其中，<code>CompareMemorySize</code> 函数只负责检查内存分配的长度，而内存的分配与释放都放在 <code>AllocMemory</code> 函数内进行。这样不仅不会导致内存的双重释放，而且完全遵从“<strong>内存资源的分配与释放应该限定在同一模块或者同一抽象层内进行</strong>”原则。</p><h2 id="返回指针进行强制类型转换"><a href="#返回指针进行强制类型转换" class="headerlink" title="返回指针进行强制类型转换"></a>返回指针进行强制类型转换</h2><p>在 C 语言中，“<code>void</code>”被称为“无类型”，而“<code>void*</code>”则被称为“无类型指针”。之所以称“<code>void*</code>”为“无类型指针”，是因为它<code>可以指向任何数据类型</code>。因此，对于任何类型“<code>T</code>”都可以转换为“<code>void</code><em>”，而“`void</em>也可以转换为任何类型“T*”。</p><p>也正是因为“<code>void</code>”的这个特征，它常被用在如下两个方面：</p><ul><li>对函数返回的限定，即如果函数没有返回值，那么应将其声明为 <code>void</code> 类型。</li><li>对函数参数的限定，即如果函数无参数，那么声明函数参数为 <code>void</code>。</li></ul><p>当然，内存管理函数也不例外，如 <code>malloc</code>、<code>realloc</code>、<code>calloc</code>、<code>alloca</code> 与 <code>aligned_alloc</code> 函数的返回都是<code>void*</code>类型。但需要特别注意的是，在使用这些内存管理函数进行内存分配时，必须将返回类型<code>void*</code>强制转换为指向被分配类型的指针。如下面的代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(<span class="number">10</span> * <span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br></pre></td></tr></table></figure><p>当然，为了能够简单调用，也可以将 malloc 函数使用 define 定义成如下形式：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MALLOC(type) ((type *)malloc(sizeof(type)))</span></span><br><span class="line"><span class="comment">/*或者*/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MALLOC(number,type) ((type *)malloc((number) * sizeof(type)))</span></span><br></pre></td></tr></table></figure><p>现在，调用就简单多了，如下面的代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *p = MALLOC(<span class="keyword">char</span>);</span><br><span class="line"><span class="comment">/*或者*/</span></span><br><span class="line"><span class="keyword">char</span> *p = MALLOC(<span class="number">10</span>, <span class="keyword">char</span>);</span><br></pre></td></tr></table></figure><p>下面的宏为大家提供了更多方便：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*malloc*/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MALLOC_ARRAY(number, type) \((type *)malloc((number)* sizeof(type)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MALLOC_FLEX(stype, number, etype) \((stype *)malloc(sizeof(stype) \</span></span><br><span class="line">+ (number)* <span class="keyword">sizeof</span>(etype)))</span><br><span class="line"><span class="comment">/*calloc*/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CALLOC(number, type) \((type *)calloc(number, sizeof(type)))</span></span><br><span class="line"><span class="comment">/*realloc*/</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> REALLOC_ARRAY(pointer, number, type) \((type *)realloc(pointer, (number)* sizeof(type)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> REALLOC_FLEX(pointer, stype, number, etype) \((stype *)realloc(pointer, sizeof(stype) \</span></span><br><span class="line">+ (number)* <span class="keyword">sizeof</span>(etype)))</span><br></pre></td></tr></table></figure><h2 id="指向一块合法的内存"><a href="#指向一块合法的内存" class="headerlink" title="指向一块合法的内存"></a>指向一块合法的内存</h2><p>在 C 语言中，只要是指针变量，那么在使用它之前必须确保该指针变量的值是一个有效的值，它能够指向一块合法的内存，并从根本上避免未分配内存或者内存分配不足的情况发生。</p><p>看下面一段示例代码：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">phonelist</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> number;</span><br><span class="line">    <span class="keyword">char</span> *name;</span><br><span class="line">    <span class="keyword">char</span> *tel;</span><br><span class="line">&#125;<span class="built_in">list</span>,*plist;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">list</span>.number = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>(<span class="built_in">list</span>.name, <span class="string">"Abby"</span>);</span><br><span class="line">    <span class="built_in">strcpy</span>(<span class="built_in">list</span>.tel, <span class="string">"13511111111"</span>);</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于上面的代码片段，在定义结构体变量 <code>list</code> 时，<strong>并未给结构体 phonelist 内部的指针变量成员“<code>char*name</code>”与“<code>char*tel</code>”分配内存</strong>。这时候的指针变量成员“<code>char*name</code>”与“<code>char*tel</code>”并没有指向一个合法的地址，从而导致其内部存储的将是一些未知的乱码。</p><p>因此，在调用 <code>strcpy</code> 函数时，如“<code>strcpy(list.name,&quot;Abby&quot;)</code>”语句会将字符串”<code>Abby</code>“向未知的乱码所指的内存上拷贝，而这块内存 <code>name</code> 指针根本就无权访问，从而导致程序出错。</p><p>既然没有给指针变量成员“<code>char*name</code>”与“<code>char*tel</code>”分配内存，那么解决的办法就是为指针变量成员分配内存，使其指向一个合法的地址，如下面的示例代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">list</span>.name = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="number">20</span>*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br><span class="line"><span class="built_in">strcpy</span>(<span class="built_in">list</span>.name, <span class="string">"Abby"</span>);</span><br><span class="line"><span class="built_in">list</span>.tel = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="number">20</span>*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br><span class="line"><span class="built_in">strcpy</span>(<span class="built_in">list</span>.tel, <span class="string">"13511111111"</span>);</span><br></pre></td></tr></table></figure><p>除此之外，下面的错误也是大家经常容易忽视的：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">phonelist</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> number;</span><br><span class="line">    <span class="keyword">char</span> *name;</span><br><span class="line">    <span class="keyword">char</span> *tel;</span><br><span class="line">&#125;<span class="built_in">list</span>, *plist;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    plist = (struct phonelist*)<span class="built_in">malloc</span>(\</span><br><span class="line">        <span class="keyword">sizeof</span>(struct phonelist));</span><br><span class="line">    <span class="keyword">if</span> (plist)</span><br><span class="line">    &#123;</span><br><span class="line">        plist-&gt;number = <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">strcpy</span>(plist-&gt;name, <span class="string">"Abby"</span>);</span><br><span class="line">        <span class="built_in">strcpy</span>(plist-&gt;tel, <span class="string">"13511111111"</span>);</span><br><span class="line">            <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="built_in">free</span>(plist);</span><br><span class="line">    plist = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不难发现，上面的代码片段虽然为结构体指针变量 <code>plist</code> 分配了内存，但是仍旧没有给结构体指针变量成员“<code>char*name</code>”与“<code>char*tel</code>”分配内存，从而导致结构体指针变量成员“<code>char*name</code>”与“<code>char*tel</code>”并没有指向一个合法的地址。因此，应该做如下修改：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">plist-&gt;name = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="number">20</span>*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br><span class="line"><span class="built_in">strcpy</span>(plist-&gt;name, <span class="string">"Abby"</span>);</span><br><span class="line">plist-&gt;tel = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="number">20</span>*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));</span><br><span class="line"><span class="built_in">strcpy</span>(plist-&gt;tel, <span class="string">"13511111111"</span>);</span><br></pre></td></tr></table></figure><p>由此可见，对结构体来说，仅仅是为结构体指针变量分配内存还是不够的，还必须为结构体成员中的所有指针变量分配足够的内存。</p><h2 id="分配足够的内存空间"><a href="#分配足够的内存空间" class="headerlink" title="分配足够的内存空间"></a>分配足够的内存空间</h2><p>对于上面的结构体指针变量 plist 的内存分配语句：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">plist = (struct phonelist*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct phonelist));</span><br></pre></td></tr></table></figure><p>如果不小心误写成如下形式会怎么样呢？</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">plist = (struct phonelist*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct phonelist*));</span><br></pre></td></tr></table></figure><p>虽然这里只是简单地将“<code>sizeof(struct phonelist)</code>”误写成了“<code>sizeof(struct phonelist*)</code>”，但将会因为结构体指针变量 <code>plist</code> 内存分配不足而导致程序的内存错误发生。类似的示例还有许多，如下面的代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">size_t</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> *p;</span><br><span class="line">    <span class="keyword">if</span> (len == <span class="number">0</span> || len &gt; SIZE_MAX / <span class="keyword">sizeof</span>(<span class="keyword">long</span>))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/*溢出处理*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    p = (<span class="keyword">long</span> *)<span class="built_in">malloc</span>(len * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">    <span class="keyword">if</span> (p == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="built_in">free</span>(p);</span><br><span class="line">    p = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的示例代码中，内存分配语句“<code>p=(long*)malloc(len*sizeof(int))</code>”使用了“<code>sizeof(int)</code>”来计算内存的大小，而不是 <code>sizeof(long)</code>，这显然是不对的，应该修改成 <code>sizeof(long)</code>，当然，也可以用“<code>sizeof(*p)</code>”。</p><p>除此之外，对于数组对象尤其要注意内存分配的问题，如下面的代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ARRAY_SIZE 10</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">datalist</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">size_t</span> number;</span><br><span class="line">    <span class="keyword">int</span> data[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">datalist</span> <span class="title">list</span>;</span></span><br><span class="line">    <span class="built_in">list</span>.number = ARRAY_SIZE;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i = <span class="number">0</span>; i &lt; ARRAY_SIZE; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">list</span>.data[i] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于上面的示例，当一个结构体中包含数组成员时，其数组成员的大小必须添加到结构体的大小中。因此，上面示例的正确内存分配方法应该按照如下方式进行：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ARRAY_SIZE 10</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">datalist</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">size_t</span> number;</span><br><span class="line">    <span class="keyword">int</span> data[];</span><br><span class="line">&#125;；</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">datalist</span> *<span class="title">plist</span>;</span></span><br><span class="line">    plist = (struct datalist *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct datalist)+<span class="keyword">sizeof</span>(<span class="keyword">int</span>) * ARRAY_SIZE);</span><br><span class="line">    <span class="keyword">if</span> (plist == <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    plist-&gt;number = ARRAY_SIZE;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i = <span class="number">0</span>; i &lt; ARRAY_SIZE; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">            plist-&gt;data[i] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由上面的几个示例代码片段可见，对于 <code>malloc</code>、<code>calloc</code>、<code>realloc</code> 与 <code>aligned_alloc</code> 内存分配函数中长度参数的大小，必须保证有足够的范围来表示对象要存储的大小。如果长度参数不正确或者可能被攻击者所操纵，将可能会出现缓冲区溢出。与此同时，不正确的长度参数、不充分的范围检查、整数溢出或截断都会导致分配长度不足的缓冲区。因此，一定要确保内存分配函数的长度参数能够合法地分配足够数量的内存。</p><h2 id="禁止执行零长度的内存分配"><a href="#禁止执行零长度的内存分配" class="headerlink" title="禁止执行零长度的内存分配"></a>禁止执行零长度的内存分配</h2><p>根据 C99 规定，如果在程序中试图调用 <code>malloc</code>、<code>calloc</code> 与 realloc 等系列内存分配函数分配长度为 0 的内存，那么其行为将是由具体编译器所定义的（如可能返回一个 <code>null</code> 指针，又或者是长度为非零的值等），从而导致产生不可预料的结果。</p><p>因此，为了保证不会将 0 作为长度参数值传给 malloc、calloc 与 realloc 等系列内存分配函数，应该对这些内存分配函数的<strong>长度参数进行合法性检查</strong>，以保证它的合法取值范围。</p><p>如下面的代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">size_t</span> len;</span><br><span class="line"><span class="comment">/*初始化len变量*/</span></span><br><span class="line"><span class="keyword">if</span> (len == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* 处理长度为0的错误 */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">int</span> *p = (<span class="keyword">int</span> *)<span class="built_in">malloc</span>(len);</span><br><span class="line"><span class="keyword">if</span> (p == <span class="literal">NULL</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*...*/</span></span><br></pre></td></tr></table></figure><h2 id="避免大型的堆栈分配"><a href="#避免大型的堆栈分配" class="headerlink" title="避免大型的堆栈分配"></a>避免大型的堆栈分配</h2><p>C99 标准引入了对变长数组的支持，如果变长数组的长度传入未进行任何检查和处理，那么将很容易被攻击者用来实施攻击，如常见的 DOS 攻击。看下面的示例代码：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">CopyFile</span><span class="params">(FILE *src, FILE *dst, <span class="keyword">size_t</span> bufsize)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buf[bufsize];</span><br><span class="line">    <span class="keyword">while</span> (fgets(buf, bufsize, src))</span><br><span class="line">    &#123;</span><br><span class="line">            <span class="keyword">if</span> （<span class="built_in">fputs</span>(buf, dst) == EOF)</span><br><span class="line">            &#123;</span><br><span class="line">                    <span class="comment">/*...*/</span></span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的示例代码中，数组“<code>char buf[bufsize]</code>”的长度将根据 <code>CopyFile</code> 函数的 <code>bufsize</code> 参数来决定，这显然不符合要求的。对于这种情况，可以通过一个 <code>malloc</code> 调用来替换掉这个变长数组。与此同时，如果 <code>malloc</code> 函数内存分配失败，还可以对返回值进行检查，从而防止程序异常终止等情况发生。如下面的示例代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">CopyFile</span><span class="params">(FILE *src, FILE *dst, <span class="keyword">size_t</span> bufsize)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (bufsize == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">char</span> *buf = (<span class="keyword">char</span> *)<span class="built_in">malloc</span>(bufsize);</span><br><span class="line">    <span class="keyword">if</span> (buf == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/*...*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (fgets(buf, bufsize, src))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">fputs</span>(buf, dst) == EOF)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/*...*/</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="built_in">free</span>(buf);</span><br><span class="line">    buf = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="避免内存分配成功，但并未初始化"><a href="#避免内存分配成功，但并未初始化" class="headerlink" title="避免内存分配成功，但并未初始化"></a>避免内存分配成功，但并未初始化</h2><p>在通常情况下，导致这种错误的主要原因有两个：</p><ul><li>没有初始化的观念。</li><li>误以为内存的默认初值全部为零，从而导致引用初值错误（如数组）。</li></ul><p>其实，内存的默认初值究竟是什么并没有统一的标准。如 <code>malloc</code> 函数分配得到的内存空间就是未初始化的，而它所分配的内存空间里可能包含出乎意料的值。因此，一般在使用该内存空间时，就需要调用函数 <code>memset</code> 来将其初始化为全 <code>0</code>。如下面的示例代码所示：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * p = <span class="literal">NULL</span>;</span><br><span class="line">p = (<span class="keyword">int</span>*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line"><span class="keyword">if</span> (p == <span class="literal">NULL</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/*...*/</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*初始化为0*/</span></span><br><span class="line"><span class="built_in">memset</span>(p, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br></pre></td></tr></table></figure><p>对于 <code>realloc</code> 函数，同样需要使用 <code>memset</code> 函数对其内存进行初始化。而对于数组，也别忘了赋初值，即便是赋零值也不可省略，千万不要嫌麻烦。</p>]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>C创建宏的注意要点</title>
    <url>/posts/3438752899/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h1 id="定义一个宏，求两个数中的最大数"><a href="#定义一个宏，求两个数中的最大数" class="headerlink" title="定义一个宏，求两个数中的最大数"></a>定义一个宏，求两个数中的最大数</h1><h2 id="合格"><a href="#合格" class="headerlink" title="合格"></a>合格</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span>  MAX(x,y)  x &gt; y ? x : y</span></span><br></pre></td></tr></table></figure><h2 id="中等"><a href="#中等" class="headerlink" title="中等"></a>中等</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX(x,y) (x) &gt; (y) ? (x) : (y)</span></span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="良好"><a href="#良好" class="headerlink" title="良好"></a>良好</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX(x,y)(&#123; \ </span></span><br><span class="line">    <span class="keyword">int</span> _x = x; \ </span><br><span class="line">    <span class="keyword">int</span> _y = y; \ </span><br><span class="line">    _x &gt; _y ? _x : _y; \ </span><br><span class="line">&#125;) </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">2</span>; </span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">6</span>; </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"max=%d"</span>,MAX(i++,j++)); </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="优秀"><a href="#优秀" class="headerlink" title="优秀"></a>优秀</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX(type,x,y)(&#123; \ </span></span><br><span class="line">    type _x = x; \ </span><br><span class="line">    type _y = y; \ </span><br><span class="line">    _x &gt; _y ? _x : _y; \ </span><br><span class="line">&#125;) </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">2</span>; </span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">6</span>; </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"max=%d\n"</span>,MAX(<span class="keyword">int</span>,i++,j++)); </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"max=%f\n"</span>,MAX(<span class="keyword">float</span>,<span class="number">3.14</span>,<span class="number">3.15</span>)); </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="流弊"><a href="#流弊" class="headerlink" title="流弊"></a>流弊</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> max(x, y) (&#123;    \</span></span><br><span class="line">    typeof(x) _x = (x); \</span><br><span class="line">    typeof(y) _y = (y); \</span><br><span class="line">    (<span class="keyword">void</span>) (&amp;_x == &amp;_y);\</span><br><span class="line">    _x &gt; _y ? _x : _y; &#125;)</span><br></pre></td></tr></table></figure><p>在这个宏定义中，使用了<code>typeof</code>(GCC的扩展)关键字<code>用来获取宏的两个参数类型</code>。干货在<code>(void) (&amp;x == &amp;y);</code>这句话，简直是天才般的设计！</p><ul><li><p>用来给用户提示一个警告，对于不同类型的指针比较，编译器会给一个警告，提示两种数据类型不同；</p></li><li><p>当两个值比较，比较的结果没有用到，有些编译器可能会给出一个warning，加个(void)后，就可以消除这个警告！</p></li></ul><h2 id="完美"><a href="#完美" class="headerlink" title="完美"></a>完美</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">max</span>(x, _x)</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> max(x, y) (&#123;				\</span></span><br><span class="line">	typeof(x) _max1 = (x);			\</span><br><span class="line">	typeof(y) _max2 = (y);			\</span><br><span class="line">	(<span class="keyword">void</span>) (&amp;_max1 == &amp;_max2);		\</span><br><span class="line">	_max1 &gt; _max2 ? _max1 : _max2; &#125;)</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __max(t1, t2, max1, max2, x, y) (&#123;              \</span></span><br><span class="line">	t1 max1 = (x);                                  \</span><br><span class="line">	t2 max2 = (y);                                  \</span><br><span class="line">	(<span class="keyword">void</span>) (&amp;max1 == &amp;max2);                        \</span><br><span class="line">	max1 &lt; max2 ? max1 : max2; &#125;)</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ___PASTE(a,b) a##b</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __PASTE(a,b) ___PASTE(a,b)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __UNIQUE_ID(prefix) __PASTE(__PASTE(__UNIQUE_ID_, prefix), __COUNTER__)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> max(x, y)                                       \</span></span><br><span class="line">	__max(typeof(x), typeof(y),                     \</span><br><span class="line">	      __UNIQUE_ID(max1_), __UNIQUE_ID(max2_),   \</span><br><span class="line">	      x, y)</span><br></pre></td></tr></table></figure><ul><li>在新版的宏中，内部的临时变量不再由程序员自己定义，而是让编译器生成一个独一无二的变量，这样就避免了同名冲突的风险。宏<strong>__UNIQUE_ID</strong>的作用就是生成了一个独一无二的变量，确保了临时变量的唯一性。关于它的使用，可以参考<a href="https://link.zhihu.com/?target=https%3A//gaomf.cn/2017/10/08/Kernel_min_max_macro/">文章</a>，写的很好。</li></ul><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul><li><a href="https://www.zhihu.com/question/22465774" target="_blank" rel="noopener">Linux三剑客知乎</a></li></ul>]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>#和##在字符串中的作用</title>
    <url>/posts/862121343/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="普通宏定义"><a href="#普通宏定义" class="headerlink" title="普通宏定义"></a>普通宏定义</h2><ul><li>普通输出</li></ul><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRINT_STR(s) printf(<span class="meta-string">"%s"</span>,s.c_str())</span></span><br><span class="line"><span class="built_in">string</span> str = <span class="string">"abcd"</span>;</span><br><span class="line">PRINT_STR(str);</span><br></pre></td></tr></table></figure><p>结果如下：abcd</p><a id="more"></a><h2 id="字符串替换"><a href="#字符串替换" class="headerlink" title="字符串替换"></a>字符串替换</h2><ul><li>将变量转换为字符串输出</li></ul><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRINT_STR(s) printf(#s <span class="meta-string">"="</span> <span class="meta-string">"%s"</span>,s.c_str())</span></span><br></pre></td></tr></table></figure><p>结果如下：str = abcd</p><ul><li>对于#s，<strong>编译器对s进行替换，并且在s前后加上双引号。</strong></li></ul><h2 id="符号连接"><a href="#符号连接" class="headerlink" title="符号连接"></a>符号连接</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRINT_TOKEN(d) printf(<span class="meta-string">"%d"</span>,token##d)</span></span><br><span class="line"><span class="keyword">int</span> token8 = <span class="number">102</span>;</span><br><span class="line">PRINT_TOKEN(<span class="number">8</span>);</span><br></pre></td></tr></table></figure><p>结果如下：102</p><ul><li>对于token##d，##将两个符号连接起来。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li>一个#字符串连接</li><li>两个#符号连接</li></ul>]]></content>
      <categories>
        <category>c</category>
      </categories>
      <tags>
        <tag>c</tag>
      </tags>
  </entry>
  <entry>
    <title>私人归档 0</title>
    <url>/posts/1934373265/</url>
    <content><![CDATA[<div id="hexo-blog-encrypt" data-wpm="抱歉, 这个密码看着不太对, 请再试试." data-whm="抱歉, 这个文章不能被校验, 不过您还是能看看解密后的内容."><div class="hbe-input-container"><input type="password" id="hbePass" placeholder="您好, 这里需要密码." /><label>您好, 这里需要密码.</label><div class="bottom-line"></div></div><script id="hbeData" type="hbeData" data-hmacdigest="8bb3f09a162c148be4479d831aebe1d1b68391096b9a12ba1552eaedad8f8e97"></script></div><script src="/lib/blog-encrypt.js"></script><link href="/css/blog-encrypt.css" rel="stylesheet" type="text/css">]]></content>
      <categories>
        <category>private</category>
      </categories>
      <tags>
        <tag>inspiration</tag>
      </tags>
  </entry>
  <entry>
    <title>Next主题配置</title>
    <url>/posts/3510939897/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p>主要记录一下next主题的一些简单的配置，以便后面更加轻松的使用next的一些配置。</p><a id="more"></a><div id="aplayer-ELCEHydI" class="aplayer aplayer-tag-marker meting-tag-marker" data-id="832907794" data-server="netease" data-type="playlist" data-mode="circulation" data-autoplay="false" data-mutex="true" data-listmaxheight="340px" data-preload="auto" data-theme="#ad7a86"></div><h2 id="下载主题"><a href="#下载主题" class="headerlink" title="下载主题"></a>下载主题</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">cd</span> hexo</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> https://github.com/theme-next/hexo-theme-next themes/next</span></span><br></pre></td></tr></table></figure><h2 id="主题说明"><a href="#主题说明" class="headerlink" title="主题说明"></a>主题说明</h2><p>在项目文件中有多个<code>_config.yml</code>文件，为方便区分。</p><ul><li><p>hexo根目录下的<code>_config.yml</code>称为<code>站点配置文件</code>。</p></li><li><p>next主题文件夹下的<code>theme/next/_config.yml</code>称为<code>主题配置文件</code>。</p></li></ul><h2 id="设置语言"><a href="#设置语言" class="headerlink" title="设置语言"></a>设置语言</h2><p>在<code>主题配置文件</code>中设置简体中文</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">language: zh-CN</span><br></pre></td></tr></table></figure><h2 id="设置Menu"><a href="#设置Menu" class="headerlink" title="设置Menu"></a>设置Menu</h2><h3 id="新建页面"><a href="#新建页面" class="headerlink" title="新建页面"></a>新建页面</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> hexo new page categories</span></span><br></pre></td></tr></table></figure><h3 id="添加type为“categories”"><a href="#添加type为“categories”" class="headerlink" title="添加type为“categories”"></a>添加type为“categories”</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">文章分类</span></span><br><span class="line"><span class="attr">date:</span> <span class="number">2019</span><span class="number">-12</span><span class="number">-16</span> <span class="number">13</span><span class="string">:59:21</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">"categories"</span></span><br><span class="line"><span class="meta">---</span></span><br></pre></td></tr></table></figure><h3 id="为文章Next主题配置指定分类hexo"><a href="#为文章Next主题配置指定分类hexo" class="headerlink" title="为文章Next主题配置指定分类hexo"></a>为文章<code>Next主题配置</code>指定分类<code>hexo</code></h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">title:</span> <span class="string">Next主题配置</span></span><br><span class="line"><span class="attr">date:</span> <span class="number">2019</span><span class="number">-12</span><span class="number">-15</span> <span class="number">10</span><span class="string">:57:12</span></span><br><span class="line"><span class="attr">categories:</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">hexo</span></span><br></pre></td></tr></table></figure><h2 id="更新主题"><a href="#更新主题" class="headerlink" title="更新主题"></a>更新主题</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">cd</span> themes/next</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git pull</span></span><br></pre></td></tr></table></figure><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://theme-next.org" target="_blank" rel="noopener">next官方文档</a></p>]]></content>
      <categories>
        <category>hexo</category>
        <category>next</category>
      </categories>
      <tags>
        <tag>next</tag>
      </tags>
  </entry>
  <entry>
    <title>Welcome to Hexo</title>
    <url>/posts/51646408/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using <code>Hexo</code>, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><a id="more"></a><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>next</tag>
      </tags>
  </entry>
</search>
